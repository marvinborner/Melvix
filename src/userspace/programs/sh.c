#include <stdint.h>
#include <stdio.h>
#include <stddef.h>
#include <common.h>
#include <unistd.h>
#include <syscall.h>
#include <gui.h>

void main()
{
	printf("[~] ");

	/* while (1) { */
	/* 	putch(getch()); */
	/* } */

	/* syscall_halt(); */
	while (1) {
		//printf("A");
	};
}