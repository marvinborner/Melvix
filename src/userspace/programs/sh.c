#include <common.h>
#include <gui.h>
#include <stddef.h>
#include <stdint.h>
#include <stdio.h>
#include <syscall.h>
#include <unistd.h>

void main()
{
	printf("[~] ");

	/* while (1) { */
	/* 	putch(getch()); */
	/* } */

	/* syscall_halt(); */
	while (1) {
		//printf("A");
	};
}